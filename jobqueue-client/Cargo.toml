[package]
name = "jobqueue-client"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = "4.3.0"
libjobqueue = { version = "0.1.0", path = "../libjobqueue" }
zmq = "0.10.0"
